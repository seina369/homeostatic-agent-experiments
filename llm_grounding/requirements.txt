# 言語モデル接続実験(llm_grounding/)の依存。Google Colab 無料枠(T4 GPU)+PyTorch 経路。
# torch は Colab に元から入っている CUDA 対応版をそのまま使う(バージョン指定なし)。
torch
transformers>=4.56
peft>=0.13
accelerate>=1.0
# 計測・分析スクリプト(run_torch_speed_probe.py, analyze_granularity.py, grpo_core.py)用
numpy
# 読み取り器(leak_probe.py の TF-IDF)用
scikit-learn
# 開発用(テストの実行のみ。実験の実行には不要): pytest
pytest
# Mac/MLX 経路(mlx_qwen_policy.py, M2 MacBook Air)は、上記の代わりに次の2つを入れる: mlx, mlx-lm
